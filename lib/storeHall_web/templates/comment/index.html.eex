
<h1>Listing Comments</h1>

<comments>
  <%= for comment <- @user.comments do %>
    <%= render "show.html", comment: comment %>
  <% end %>
</comments>

<h3>Add Comment</h3>
<%= render "form.html", comment: %{
  id: nil,
  author_id: @conn.assigns[:logged_user].id,
  user_id: @user.id
} %>
